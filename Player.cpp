// Imported Files 
#include "Player.h"

// Imported Libraries
#include <string>

using namespace std;

// Constructor
Player::Player(string name, string advisor, int path) {
    playerName = name;
    playerAdvisor = advisor;
    playerPath = path;
}